console.log("app started");

setTimeout(() => console.log("inside timer handler of setTimeout 3000"), 3000);

setTimeout(() => console.log("inside timer handler of setTimeout 0"), 0);

while (true) {}
